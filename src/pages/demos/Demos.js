import styles from './Demos.module.scss';

const Demos = () => {
    return (
        <div className={styles.demos}>
            <h1>Recent Work</h1>
        </div>
    );
};

export default Demos;
